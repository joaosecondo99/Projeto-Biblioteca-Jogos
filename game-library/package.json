{
  "name": "game-library",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 3001",
    "start": "concurrently \"npm run server\" \"vite preview --port $PORT --host\"",
    "dev:full": "concurrently \"npm run server\" \"npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "json-server": "^1.0.0-beta.15",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "axios": "^1.18.0"
  }
}
